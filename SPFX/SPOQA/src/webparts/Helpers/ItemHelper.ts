// This is a place holder
/*import {SPHttpClient,ISPHttpClientOptions} from '@microsoft/sp-http';
import SPOQAHelper from './SPOQAHelper';
import RestAPIHelper from './RestAPIHelper';

export enum ItemType 
{
    ListItem=0,
    Document=1,
    Folder=2,
    UnKnown=-1
}

export default class ItemHelper
{
    public static async TryGetItemByFullUrl(spHttpClient:SPHttpClient, siteAbsoluteUrl:string, listRootFolder:string, fullDocmentPath:string)
    {
        siteAbsoluteUrl = decodeURI(siteAbsoluteUrl);
        listRootFolder = decodeURI(listRootFolder);
        fullDocmentPath = decodeURI(fullDocmentPath);        
    }
}*/

